{% if block.settings.enable %}
<div id="app-reviews-root"
     data-product-id="{{ product.id }}"
     data-product-name="{{ product.title | escape }}"
     data-shop="{{ shop.permanent_domain }}"
     data-api-base="{{ block.settings.api_base_url | default: 'https://judgme.onrender.com' | escape }}">
</div>
<script src="{{ 'review-widget.js' | asset_url }}" defer></script>
{% endif %}

{% schema %}
{
  "name": "Product Reviews",
  "target": "section",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable",
      "label": "Enable reviews",
      "default": true
    },
    {
      "type": "text",
      "id": "api_base_url",
      "label": "App API base URL",
      "default": "https://judgme.onrender.com",
      "info": "Deployed app origin with no trailing slash (use your shopify app dev tunnel URL while testing locally)."
    }
  ]
}
{% endschema %}
